<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#393939"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.hardeep.something.friendRequest">

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/requestListView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="10dp"
        android:divider="#006064"
        android:dividerHeight="2dp"
        android:layout_below="@+id/requestEditText" />

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/requestPBar"
        android:indeterminate="true"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <AutoCompleteTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/requestEditText"
        android:textColor="#ecf0f1"
        android:hint="Enter Username"
        android:textColorHint="#ecf0f1"
        android:inputType="textNoSuggestions"
        android:layout_marginTop="20dp"
        android:textStyle="normal"
        android:typeface="serif"
        android:textSize="18sp"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/requestListView" />

</RelativeLayout>
